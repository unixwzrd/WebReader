#!/usr/bin/env bash
set -euo pipefail

project_dir=$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)

xcodebuild \
  -project "$project_dir/safari/WebReader/WebReader.xcodeproj" \
  -scheme WebReader \
  -configuration Debug \
  -derivedDataPath "$project_dir/build/DerivedData" \
  build
